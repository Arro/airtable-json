{
  "name": "airtable-json",
  "version": "1.0.0",
  "description": "get airtable records, but just in raw json form",
  "main": "airtable-json.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.5.3"
  }
}
